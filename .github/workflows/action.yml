name: Cypress Test

on: [push]

jobs:
  cypress_run:
    runs-on: macOS-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          build: npm install
          start: npm start

