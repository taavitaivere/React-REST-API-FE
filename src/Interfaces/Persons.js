export interface personData {
    id: number;
    name: string;
    email: string;
    avatar: string;
    token: string;
}

export interface editedPersonData {
    name: string;
    email: string;
    avatar: string;
    token: string;
}

export interface newPersonData {
    name: string;
    email: string;
    avatar: string;
    token: string;
}
