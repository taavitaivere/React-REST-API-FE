export interface personData {
    id: number;
    name: string;
    email: string;
    avatar: string;
}

export interface editedPersonData {
    name: string;
    email: string;
    avatar: string;
}

export interface newPersonData {
    name: string;
    email: string;
    avatar: string;
}
